version: 1.0.2
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    npm install
    pip install -r requirements.txt
dependencies:
  {}
icon: ./icon.png
name: ssh
displayName: SSH
repository: https://github.com/oomol-flows/ssh
description: Exec Command, Upload amd Download in SSH
releaseNotes: update release type
releaseType: package
